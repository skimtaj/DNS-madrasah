<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Reset Password — SecureApp</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #00A884;
            --primary-dark: #008F70;
            --bg-gradient: linear-gradient(135deg, #E6FFFA, #F0FDF4);
            --card-bg: #ffffffee;
            --muted: #6b7280;
        }

        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--bg-gradient);
            font-family: 'Inter', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;
            padding: 1.5rem;
        }

        .fp-card {
            width: 100%;
            max-width: 420px;
            border-radius: 18px;
            background: var(--card-bg);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
            overflow: hidden;
            border: 1px solid rgba(0, 0, 0, 0.05);
            animation: fadeIn 0.6s ease;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .fp-header {
            padding: 2rem 1.5rem 1rem 1.5rem;
            text-align: center;
        }

        .brand-logo {
            width: 80px;
            height: 80px;
            border-radius: 16px;
            background: linear-gradient(135deg, rgba(0, 168, 132, 0.15), rgba(0, 168, 132, 0.05));
            border: 1px solid rgba(0, 168, 132, 0.25);
            margin: 0 auto 1rem;
            display: grid;
            place-items: center;
            overflow: hidden;
        }

        .brand-logo img {
            width: 60%;
            height: 60%;
            object-fit: contain;
        }

        h1 {
            font-size: 1.3rem;
            font-weight: 600;
            margin-bottom: 0.3rem;
        }

        p.small-muted {
            color: var(--muted);
            font-size: 0.9rem;
        }

        .input-icon {
            position: relative;
            margin-bottom: 1.1rem;
        }

        .input-icon i {
            position: absolute;
            left: 14px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--muted);
            font-size: 15px;
        }

        .input-icon .form-control {
            padding-left: 42px;
            padding-top: 0.55rem;
            padding-bottom: 0.55rem;
            border-radius: 9px;
            border: 1px solid #d1d5db;
            transition: all 0.3s ease;
            font-size: 0.95rem;
        }

        .input-icon .form-control:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(0, 168, 132, 0.15);
        }

        .btn-primary {
            background: var(--primary);
            border: none;
            padding: 0.6rem 1rem;
            border-radius: 9px;
            font-weight: 500;
            transition: all 0.25s ease;
            box-shadow: 0 3px 10px rgba(0, 168, 132, 0.25);
            font-size: 0.95rem;
        }

        .btn-primary:hover {
            background: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 168, 132, 0.35);
        }



        .password-strength-bar {
            height: 100%;
            width: 0%;
            border-radius: 2px;
            transition: width 0.3s ease, background-color 0.3s ease;
        }



        .thin-footer {
            padding: 0.8rem 1.5rem;
            font-size: 0.85rem;
            color: var(--muted);
            background: #fafafa;
            border-top: 1px solid rgba(0, 0, 0, 0.05);
            text-align: center;
        }

        a.link-primary {
            color: var(--primary);
            text-decoration: none;
        }

        a.link-primary:hover {
            text-decoration: underline;
        }

        @media (max-width: 576px) {
            .fp-card {
                border-radius: 12px;
                max-width: 100%;
            }

            .input-icon .form-control {
                padding-top: 0.5rem;
                padding-bottom: 0.5rem;
                font-size: 0.9rem;
            }

            .btn-primary {
                font-size: 0.9rem;
                padding: 0.55rem 0.8rem;
            }
        }
    </style>
</head>

<body>
    <main class="fp-card">
        <div class="fp-header">
            <div class="brand-logo">
                <img src="https://cdn-icons-png.flaticon.com/512/3063/3063796.png" alt="SecureApp Logo">
            </div>
            <h1>Reset your password</h1>
            <p class="small-muted">Enter a new password below to secure your account.</p>
        </div>

        <div class="fp-body px-4 pb-4">
            <form id="resetForm" method="post" action="/dnsmadrasah.org/reset-password/<%=adminSourse._id%>">
                <div class="input-icon">
                    <i class="fas fa-key"></i>
                    <input type="password" id="password" name="newPassword" class="form-control"
                        placeholder="New password" required>
                    <div class="password-strength">
                        <div class="password-strength-bar" id="passwordStrengthBar"></div>
                    </div>
                </div>


                <button type="submit" class="btn btn-primary w-100 mt-2">
                    <i class="fas fa-check me-2"></i>Reset Password
                </button>

                <div class="text-center mt-3 small-muted">
                    <a href="/dnsmadrasah.org/admin-login" class="link-primary"><i
                            class="fas fa-arrow-left me-1"></i>Back to sign in</a>
                </div>
            </form>
        </div>

        <div class="thin-footer">
            Make sure your new password is strong and unique.
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>

    </script>
</body>

</html>