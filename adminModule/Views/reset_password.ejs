<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Reset Password — SecureApp</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #00A884;
            --muted: #6b7280;
            --card-bg: rgba(255, 255, 255, 0.95);
        }

        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: white;
            font-family: Inter, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;
            padding: 1.5rem;
        }

        .fp-card {
            width: 100%;
            max-width: 420px;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.65);
            background: var(--card-bg);
            backdrop-filter: blur(6px);
            overflow: hidden;
        }

        .fp-header {
            padding: 1.5rem 1.5rem 0.25rem 1.5rem;
            display: flex;
            gap: 0.75rem;
            align-items: center;
        }

        .brand-badge {
            width: 56px;
            height: 56px;
            border-radius: 12px;
            display: grid;
            place-items: center;
            background: linear-gradient(135deg, rgba(0, 168, 132, 0.15), rgba(0, 168, 132, 0.08));
            border: 1px solid rgba(0, 168, 132, 0.2);
            font-size: 20px;
            color: var(--primary);
        }

        .fp-body {
            padding: 1rem 1.5rem 1.5rem 1.5rem
        }

        h1 {
            font-size: 20px;
            margin: 0 0 6px 0
        }

        p.lead {
            margin: 0 0 16px 0;
            color: var(--muted);
            font-size: 0.95rem
        }

        .form-control:focus {
            box-shadow: none;
            border-color: var(--primary)
        }

        .small-muted {
            color: var(--muted);
            font-size: 0.85rem
        }

        .action-row {
            display: flex;
            gap: 0.75rem;
            align-items: center
        }

        .btn-primary {
            background: var(--primary);
            border: none;
            padding: .65rem 1rem;
            border-radius: 10px
        }

        .thin-footer {
            padding: 0.8rem 1.5rem;
            background: transparent;
            border-top: 1px solid rgba(15, 23, 42, 0.08);
            text-align: center
        }

        .input-icon {
            position: relative
        }

        .input-icon i {
            position: absolute;
            left: 14px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--muted);
            font-size: 15px;
        }

        .input-icon .form-control {
            padding-left: 42px
        }

        @media (max-width:420px) {
            body {
                padding: 1rem
            }

            .fp-card {
                max-width: 100%;
                border-radius: 12px
            }

            .fp-header {
                padding: 1rem
            }

            .fp-body {
                padding: 0.75rem 1rem
            }

            h1 {
                font-size: 18px
            }

            p.lead {
                font-size: 0.88rem
            }

            .brand-badge {
                width: 48px;
                height: 48px;
                font-size: 18px
            }

            .btn-primary {
                padding: .55rem .8rem;
                font-size: 0.95rem;
                border-radius: 8px
            }

            .input-icon .form-control {
                padding-left: 40px;
                padding-top: .55rem;
                padding-bottom: .55rem
            }

            .thin-footer {
                padding: 0.6rem;
                font-size: 0.88rem
            }
        }
    </style>
</head>

<body>
    <main class="fp-card">
        <div class="fp-header">
            <div class="brand-badge">
                <i class="fas fa-lock"></i>
            </div>
            <div>
                <h1>Reset your password</h1>
                <p class="small-muted">Enter your new password below to secure your account.</p>
            </div>
        </div>

        <div class="fp-body">
            <form id="resetForm" novalidate method="post">
                <div class="mb-3 input-icon">
                    <i class="fas fa-key"></i>
                    <label for="password" class="form-label visually-hidden">New Password</label>
                    <input type="password" id="password" name="newPassword" class="form-control"
                        placeholder="New password" required>
                    <div class="invalid-feedback">Please enter a new password.</div>
                </div>

                <div class="mb-3 action-row">
                    <button type="submit" class="btn btn-primary w-100" id="resetBtn">
                        <span id="btnText"><i class="fas fa-check me-2"></i>Reset Password</span>
                    </button>
                </div>

                <div class="text-center small-muted">
                    <a href="#" id="backToLogin" class="link-primary">Back to sign in</a>
                </div>
            </form>
        </div>

        <div class="thin-footer small-muted">
            Make sure your new password is strong and unique.
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>

    </script>
</body>

</html>